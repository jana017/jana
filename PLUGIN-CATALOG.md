# NivX Forge — Decoder Plugin Catalog

> Auto-generated by `backend/tools/generate_plugin_catalog.py`. Do not edit by hand — regenerate to update.

**Total plugins**: 41   ·   
**Categories**: 8   ·   
**Golden regression samples**: 7

## Table of contents
- [Compression](#compression) — 3 plugins
- [Cryptography](#cryptography) — 2 plugins
- [Deobfuscation](#deobfuscation) — 6 plugins
- [Encoding](#encoding) — 11 plugins
- [Extractors](#extractors) — 8 plugins
- [Hashing](#hashing) — 4 plugins
- [Structured](#structured) — 3 plugins
- [Utilities](#utilities) — 4 plugins

## Compression

| ID | Name | Description | Detect signature |
| --- | --- | --- | --- |
| `gzip-decompress` | Gzip Decompress | Decompress gzip-compressed bytes (magic 1F 8B). | `return 0.95 if data[:2] == b"\x1f\x8b" else 0.0` |
| `gzip-decompress-b64` | Base64 → Gzip Decompress | One-shot: base64 decode then gzip decompress. | `` |
| `zlib-decompress` | Zlib Decompress | Decompress zlib-compressed bytes (magic 78 9C / 78 DA). | `return 0.0` |

## Cryptography

| ID | Name | Description | Detect signature |
| --- | --- | --- | --- |
| `rot13` | ROT13 | Caesar cipher, shift 13. | `` |
| `xor` | XOR | XOR bytes with a repeating key. Provide `key` param. | `` |

## Deobfuscation

| ID | Name | Description | Detect signature |
| --- | --- | --- | --- |
| `cmd-deobfuscate` | CMD Deobfuscate | Strip `^` escape carets, unwrap simple `!var!` expansions, remove wrapping quotes. | `` |
| `cmd-strip-carets` | Strip CMD Caret Escapes | Remove `^` intra-token escapes used in Windows CMD to bypass keyword detection (e.g. `p^o^w^e^r^shell`). | `return 0.0` |
| `defang` | Defang IOCs | Neutralize URLs/emails so they aren't clickable (http → hxxp, . → [.]). | `` |
| `js-deobfuscate` | JavaScript / CharCode Deobfuscate | Collapse string concat, resolve fromCharCode() / unescape(), decode \xNN / \uNNNN. | `return 0.0` |
| `powershell-deobfuscate` | PowerShell Deobfuscate | Strip PS escape chars (` and ^), collapse string concat, un-double quotes. | `` |
| `refang` | Refang IOCs | Reverse common IOC defanging (hxxp → http, [.] → ., [at] → @). | `` |

## Encoding

| ID | Name | Description | Detect signature |
| --- | --- | --- | --- |
| `base64-decode` | Base64 Decode | Decode Base64 (standard alphabet). Lenient: strips noise & re-pads. | `return 0.0` |
| `base64-encode` | Base64 Encode | Encode to Base64. | `` |
| `base64url-decode` | Base64 URL Decode | Decode URL-safe Base64 (- and _ instead of + and /). | `` |
| `hex-decode` | Hex Decode | Convert hex string to bytes. Strips whitespace & separators. | `return 0.0` |
| `hex-encode` | Hex Encode | Convert bytes to hex string. | `` |
| `html-entity-decode` | HTML Entity Decode | Decode HTML entities (&amp;, &#65;, &#x41;) to their characters. | `return 0.0` |
| `unicode-escape-decode` | Unicode Escape Decode | Decode \uXXXX and \xXX escape sequences. | `return 0.0` |
| `url-decode` | URL Decode | Percent-decode URL-encoded bytes (%XX). | `return 0.0` |
| `url-encode` | URL Encode | Percent-encode bytes for URL usage. | `` |
| `utf16be-decode` | UTF-16BE Decode | Decode bytes as UTF-16 Big Endian to UTF-8 text. | `return 0.92 if _looks_utf16be(data) else 0.0` |
| `utf16le-decode` | UTF-16LE Decode | Decode bytes as UTF-16 Little Endian to UTF-8 text. | `return 0.92 if _looks_utf16le(data) else 0.0` |

## Extractors

| ID | Name | Description | Detect signature |
| --- | --- | --- | --- |
| `extract-bash-base64-pipe` | Extract bash `base64 -d` piped payload | Find `echo <b64> \| base64 -d` (or `openssl enc -d -base64`) and extract the base64 blob. | `return 0.95 if _BASH_B64_RE.search(text) else 0.0` |
| `extract-bash-hex-pipe` | Extract bash `xxd -r -p` piped payload | Find `echo <hex> \| xxd -r -p` and extract the hex blob for downstream hex-decode. | `return 0.95 if _BASH_HEX_RE.search(text) else 0.0` |
| `extract-fromb64string` | Extract [Convert]::FromBase64String Payload | Find inline `[Convert]::FromBase64String("<base64>")` in PowerShell scripts (fileless staging) and extract just the base64 blob. | `return 0.95 if _FROMB64_RE.search(text) else 0.0` |
| `extract-notepad-session` | Extract Notepad /SESSION: Path | Find `Notepad.exe /SESSION:<base64>` args (Windows 11 Notepad state) and extract the referenced file path from the base64 UTF-16LE payload. | `return 0.96 if _NOTEPAD_SESSION_RE.search(text) else 0.0` |
| `extract-powershell-encoded` | Extract PowerShell Encoded Command | Find `powershell -e/-enc/-EncodedCommand <base64>` and extract just the payload. | `return 0.97 if _PS_ENC_RE.search(text) else 0.0` |
| `extract-python-b64decode` | Extract Python `base64.b64decode(...)` Payload | Find inline `base64.b64decode(b'<base64>')` or `__import__('base64').b64decode(...)` in Python `-c` one-liners (fileless staging) and extract just the base64 blob. Handles `__im… | `return 0.96 if _PY_B64DECODE_RE.search(text) else 0.0` |
| `extract-quoted-b64-blob` | Extract Largest Quoted Base64 Blob (fallback) | Last-resort extractor — pulls the largest base64 blob (200+ chars) from any Python-style string literal. Catches obfuscated wrappers where the specific `b64decode()` idiom has b… | `return 0.0` |
| `extract-strings` | Extract Strings | Extract printable ASCII + UTF-16LE strings (like `strings` binary tool). | `` |

## Hashing

| ID | Name | Description | Detect signature |
| --- | --- | --- | --- |
| `hash-md5` | Hash (MD5) | Compute MD5 digest of input. | `` |
| `hash-sha1` | Hash (SHA1) | Compute SHA1 digest of input. | `` |
| `hash-sha256` | Hash (SHA256) | Compute SHA256 digest of input. | `` |
| `hash-sha512` | Hash (SHA512) | Compute SHA512 digest of input. | `` |

## Structured

| ID | Name | Description | Detect signature |
| --- | --- | --- | --- |
| `json-minify` | JSON Minify | Collapse JSON to a single compact line. | `` |
| `json-pretty` | JSON Pretty | Pretty-print JSON with configurable indent. | `return 0.0` |
| `xml-pretty` | XML Pretty | Pretty-print XML with indentation. | `return 0.0` |

## Utilities

| ID | Name | Description | Detect signature |
| --- | --- | --- | --- |
| `lowercase` | To Lowercase | Lowercase all text. | `` |
| `normalize-whitespace` | Normalize Whitespace | Collapse repeated whitespace into single spaces. | `` |
| `remove-nulls` | Remove Null Bytes | Strip all null (0x00) bytes. | `` |
| `reverse` | Reverse | Reverse the byte sequence. | `` |

## Golden regression samples

These payloads run through `auto_decode()` + `refine()` on every HealthBot scan. If any of them fail to decode, `decoder_coverage` turns critical.

| Sample ID | Expected substring | Payload preview |
| --- | --- | --- |
| `powershell_frombase64string` | `Hello World` | `$x=[Convert]::FromBase64String('SGVsbG8gV29ybGQh');IEX` |
| `bash_echo_base64_pipe` | `Hello Malware` | `echo SGVsbG8gTWFsd2FyZQ== \| base64 -d` |
| `python_b64decode_exec` | `import os` | `-c exec(__import__('base64').b64decode(b'aW1wb3J0IG9zLHN5cwo=').decode())` |
| `cmd_caret_obfuscation` | `powershell` | `p^o^w^e^r^shell.exe -nop -w hidden -c whoami` |
| `hex_string` | `Hello World` | `48656c6c6f20576f726c64` |
| `b64_padding_no_corruption` | `import os` | `-c "exec(__import__('base64').b64decode(b'aW1wb3J0IG9zLHN5cwpvcy5jaGRpcihvcy5wYXRoLmRpc…` |
| `python_import_bare_variant` | `import os` | `-c "exec(import('base64').b64decode(b'aW1wb3J0IG9zLHN5cwo=').decode())"` |

*Analysts can add custom samples via Admin → Master → Overview → Regression Suite. They're stored in Mongo (`healthbot_regression_samples`) and merged into every scan.*

---
*Last regenerated: run `python backend/tools/generate_plugin_catalog.py` any time.*